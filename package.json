{
  "name": "cra-sports-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para el proyecto CRA Sports",
  "scripts": {
    "dev:backend": "npm run dev --prefix cra-sports-backend",
    "dev:frontend": "npm run dev --prefix cra-sports-frontend-web",
    "kill:ports": "npx kill-port 3001 5173 || echo 'Puertos ya libres o no se pudieron limpiar.'",
    "dev": "npm run kill:ports && concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "install:all": "npm install && npm install --prefix cra-sports-backend && npm install --prefix cra-sports-frontend-web"
  },
  "workspaces": [
    "cra-sports-backend",
    "cra-sports-frontend-web"
  ],
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17"
  }
}
